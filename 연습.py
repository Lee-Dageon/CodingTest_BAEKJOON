# 십자말 풀이 문제
# A는 가로로 출력
# 일단 모든 칸을 .으로 출력하기
# A와 B를 비교한 후, B에 해당되는 문자 인덱스 행부터 A를 출력
# B에 해당하는 열부터 B를 출력
# join 이라는 함수 사용

A = input().split() #문자열을 리스트로 변환
B = input().split()

L = [['.']*len(A)] for _ in range(len(B))]




